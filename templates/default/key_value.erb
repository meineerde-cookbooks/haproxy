<% extend HAProxy::TemplateHelpers %>
<% data(@data_path).reject{ |k, v| [false,nil].include?(v) }.sort.each do |key, value| %>
<% Array(value).each do |v| %>
<%= key %> <%= v unless v == true %>
<% end %>
<% end %>
